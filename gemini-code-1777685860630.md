# 專案名稱：穩健型波段防禦大腦 (Smart-Swing-Agent)
## 1. 專案概述
這是一個專為「時間有限、重視防禦與勝率」的投資人打造的全自動化台/美股追蹤與虛擬交易系統。系統嚴格遵守「不輕易出手、買在保護區」的邏輯，結合 Telegram 即時推播與 RWD 視覺化戰情室。所有盤後分析數據必須持久化儲存，以利追蹤長期趨勢與策略回測。

## 2. 技術棧 (100% 免費開源方案)
*   **核心語言**：Python 3.10+
*   **資料庫**：Supabase (PostgreSQL) - 用於儲存歷史分析數據、虛擬交易紀錄與使用者設定。
*   **後端與排程**：`APScheduler` 處理定時任務，`python-telegram-bot` 處理對話互動。
*   **前端 RWD 網頁**：`Streamlit` (部署於 Streamlit Community Cloud)。
*   **主要數據源**：
    *   台股盤後籌碼：台灣證交所 (TWSE) OpenAPI
    *   即時報價/歷史 K 線：`yfinance` 套件
    *   美股輔助：Finnhub API (免費版)

## 3. 資料庫 Schema 設計 (Supabase)
系統需具備以下三張核心資料表：

1.  `daily_analysis` (每日盤後分析庫 - 用於趨勢追蹤)
    *   `id` (UUID), `date` (Date), `ticker` (String), `type` (tw/us)
    *   `close_price` (Float), `volume` (Int), `ma_20` (Float), `ma_60` (Float)
    *   `institutional_net_buy` (Int, 法人買賣超張數)
    *   `signal_type` (String, 例如: '突破均線', '底部爆量', '外資連買')
    *   `is_large_cap` (Boolean, 是否為權值股)
2.  `paper_trades` (虛擬交易紀錄)
    *   `id` (UUID), `ticker` (String), `buy_date` (Date), `buy_price` (Float)
    *   `stop_loss_price` (Float, 防守線), `status` (String: 'OPEN', 'CLOSED')
    *   `sell_date` (Date, Nullable), `sell_price` (Float, Nullable), `pnl_percent` (Float)
3.  `user_settings` (使用者偏好設定)
    *   `telegram_chat_id` (String, Primary Key)
    *   `large_cap_only` (Boolean, 預設 True：只掃描權值龍頭股)
    *   `risk_tolerance_percent` (Float, 預設 5.0：停損趴數)

## 4. 後端核心模組與業務邏輯

### 模組 A：盤後數據抓取與分析引擎 (排程任務)
*   **觸發時間**：每交易日下午 17:00 (台股)、早上 08:00 (美股)。
*   **邏輯**：
    1. 抓取當日收盤價、成交量與三大法人籌碼。
    2. **防禦型過濾器**：優先篩選 `is_large_cap == True` 的股票（如台灣 0050 成分股）。
    3. **策略計算**：
        *   **策略1 (大人建倉)**：近 3 日法人連續買超 且 股價站上 20MA。
        *   **策略2 (恐慌抄底)**：股價跌破 60MA 且 當日爆出 5 日均量 2 倍以上之長下影線。
    4. 將符合條件的標的與技術數據寫入 `daily_analysis` 表，避免隔日重算。

### 模組 B：Telegram Bot 互動介面
*   **身分驗證**：硬編碼綁定專屬 `chat_id`，拒絕陌生人操作。
*   **排程推播**：分析引擎跑完後，讀取 `daily_analysis`，將今日符合策略的清單推播至 Telegram。
*   **指令清單**：
    *   `/settings`：切換 `large_cap_only` 狀態 (防割韭菜開關)。
    *   `/paper_buy [股票代號] [防守價格]`：記錄一筆虛擬買單寫入 `paper_trades`。
    *   `/paper_sell [股票代號]`：平倉並計算 PnL 更新至資料庫。
    *   `/portfolio`：回報目前未平倉部位的總損益。

### 模組 C：盤中防守監控 (排程任務)
*   **觸發時間**：盤中每 30 分鐘檢查一次。
*   **邏輯**：抓取 `paper_trades` 狀態為 'OPEN' 的股票現價。若現價低於 `stop_loss_price`，立刻透過 Telegram 發送緊急停損通知。

## 5. 前端 RWD 網頁模組 (Streamlit)
*   讀取 Supabase 資料，提供以下三個視覺化分頁：
    1.  **儀表板 (Dashboard)**：顯示目前大盤情緒 (VIX)、`paper_trades` 總體勝率與資金曲線圖。
    2.  **虛擬庫存 (Portfolio)**：以表格顯示目前持倉，標示現價距離停損點的緩衝空間 (%)。
    3.  **趨勢雷達 (Screener)**：拉取 `daily_analysis` 的歷史資料，用折線圖畫出特定股票過去一個月的「法人買賣超 vs 股價」趨勢對比。